// let a= [1,2,3];

//  a[0]= "hello";

// console.log(a);


let a= "luffy";

a[0]= "a";
// auffy
console.log(a);

// In array we can replace the element and in strings we cannot replace the element.

// This way of not able to replace a string element is called immutability.

