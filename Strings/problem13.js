let str= "masai" // revers this string.
 //"iasam" --->output.


//if I run the loop from last index to first index and add each element into a new empty string, then I will be able to get the output.

let b= "";

for(let i= str.length-1; i>=0; i--){
  b= b+ str[i];
}

console.log(b);


//home work part-1

// There is a string and the length of the string is always even;

// let str= "school";
//hcsloo